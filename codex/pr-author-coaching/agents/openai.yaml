interface:
  display_name: "PR Author Coaching"
  short_description: "Coach engineers from recent PR history."
  default_prompt: "Use $pr-author-coaching to analyze one or more GitHub users' recent PRs, inspect review history and historical PR code quality, and summarize recurring problems, recurring strengths, and coaching guidance."
policy:
  allow_implicit_invocation: true
