interface:
  display_name: "APEX Agent Delegation"
  short_description: "Prepare draft-PR APEX code handoffs."
  default_prompt: "Use $apex-agent-delegation to prepare a bounded APEX code-writing handoff that opens a draft PR."
policy:
  allow_implicit_invocation: true
