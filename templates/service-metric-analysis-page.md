# Executive Summary

- Service / component:
- Environment:
- Exact history window:
- Primary conclusion:
- Most important limitation:

# Scope And Methodology

- Repo path inspected:
- Dynatrace environment:
- Production filter or exact metric filter:
- Fixed windows used:
- Notes on scalar-vs-window consistency:

# Code Path And Telemetry Semantics

- Entry path:
- Metric emitter path:
- Important files inspected:
- What each metric means:
- What each key dimension means:
- Conditions where the metric emits:
- Conditions where the metric does not emit:

# Metric Inventory

- Include one row per metric with:
  - metric name
  - meaning
  - retained Dynatrace dimensions
  - key caveats

# Full-History Summary

- Total event count:
- Average per day:
- Monthly-equivalent average:
- Duration summary:
- Additional summary bullets:

# Historical Trend

- Include a table with:
  - fixed window
  - day count
  - total events
  - average per day
  - requested outcome counts
  - requested percentages

# Dimension Breakdowns

- `isbypassed`:
- `changetype`:
- `testcohort`:
- Other retained dimensions:

# Limitations And Caveats

- Distinct-customer support:
- Dimension-coverage gaps:
- Partial windows:
- Other caveats:

# Exact DQL Used

- Include the exact DQL or metric query shapes used for the core evidence.
